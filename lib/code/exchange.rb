module Code
  class Exchange
    def initialize
    end

    def enqueue(key, data={})
    end

    def dequeue(key)
    end

    def reply(key, data={})
    end

    def send(key, data={})
    end

    def receive(key)
    end

    def set(key, value)
    end

    def get(key)
    end

    def del(key)
    end
  end
end