#!/usr/bin/env ruby
require "./lib/code"

STDOUT.sync = true
puts "Monitor starting..."
Log.log(monitor: true, starting: true)

cls = Code::Monitor
cls = Code::HerokuMonitor if ENV["RACK_ENV"] == "production"
cls.new("bin/backend").run!