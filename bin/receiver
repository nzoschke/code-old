#!/usr/bin/env ruby
app_dir = File.expand_path(File.join(File.dirname(__FILE__), '..'))
if ENV["RACK_ENV"] == "production" && app_dir == "/app"
  puts "Relocating /app to /tmp"
  `mv /app /tmp`
  Dir.chdir("/tmp/app")
end

require "./lib/code"
Code::Receiver.start!