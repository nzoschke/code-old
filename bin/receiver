#!/usr/bin/env ruby
system <<-EOF
  if [ "$RACK_ENV" == "production" ] && [ $APP_DIR == /app ]; then
    echo "Relocating /app to /tmp"
    BIN_DIR=/tmp/app/bin
    APP_DIR=/tmp/app
    mv /app /tmp
    cd $APP_DIR
  fi
EOF

require "./lib/code"
Code::Receiver.start!